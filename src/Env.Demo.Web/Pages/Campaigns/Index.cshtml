@page
@model Env.Demo.Web.Pages.Campaigns.IndexModel
@{
}

@if (Model != null && Model.campaigns != null)
{
    foreach (var item in Model.campaigns)
    {
        @(item.Subject==null || item.Subject=="" ? " no subject":item.Subject)  @item.PercentSent<br />
    }
}
